
class ContactModel{

  String name;
  String uid;
  String phone;
  bool select = false;

  ContactModel(
  {required this.name, required this.uid, required this.phone, required this.select}
      );
}